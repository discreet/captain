machine:
  environment:
    http_proxy: http://proxy.kdc.capitalone.com:8099
    https_proxy: http://proxy.kdc.capitalone.com:8099
    HTTP_PROXY: http://proxy.kdc.capitalone.com:8099
    HTTPS_PROXY: http://proxy.kdc.capitalone.com:8099
  ruby:
    version: ruby-2.2.4

dependencies:
  override:
    - 'rvm-exec ruby-2.2.4 bundle install'

test:
  post:
    - 'bundle exec rubocop'
